package com.pablogb.psychologger.model.enums;

public enum AuditAction {
    VIEW, CREATE, UPDATE, DELETE, LOGIN, LOGOUT,
    FLAG, CLEAR_FLAG, ASSIGN, UNASSIGN,
    MARK_PAID, WRITE_OFF, REACTIVATE,
    ORG_SETTINGS_UPDATE
}
