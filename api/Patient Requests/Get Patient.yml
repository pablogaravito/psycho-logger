info:
  name: Get Patient
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/patients/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
